# 比赛名称
name: "2024年足球友谊赛"

# 主视频文件
main_video: "game.mp4"

# 参赛队伍信息
teams:
  - name: "大一班"
    color: "红色"  # 队伍颜色名称（用于解说）
    code: "YIBAN"  # 队伍代码
    score: 0       # 上一节比赛结束时的比分（可选，默认0）
  - name: "大二班"
    color: "蓝色"
    code: "ERBAN"
    score: 1


# 剪辑相关配置
# description: "这场球赛对于大一班来说非常重要，赢得这场比赛将获得晋级下一轮的机会" # 比赛描述（可选）
# logo_img: "logo.png"          # 比赛logo图片（可选，默认logo.png）
# logo_video: "logo.mp4"        # 比赛logo动画视频（可选，默认logo.mp4）
# bgm: "bgm.mp3"               # 比赛集锦背景音乐（可选，默认bgm.mp3）

# 时间相关配置
# prev_time: 0                  # 上一节比赛结束时间（可选，默认0）
# quarter: 2                    # 当前比赛是第几节（可选，默认1）

# 解说员设置
# narrator: "云说"              # 解说员名称（可选，默认云说）

# 记分牌配置
# scoreboard: "scoreboard.yaml" # 记分牌配置文件（可选，默认scoreboard.yaml）